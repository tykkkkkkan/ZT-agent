# 中渔天下 ZT-agent 依赖清单
# 安装：pip install -r requirements.txt

# Web 框架
Django==5.2.17

# 数据库（MySQL）
PyMySQL==1.2.0

# 认证（JWT）
djangorestframework==3.18.0
djangorestframework-simplejwt==5.5.1
PyJWT==2.13.0

# 跨域
django-cors-headers==4.9.0

# 后台管理 UI（Unfold，替代早期 SimpleUI）
django-unfold==0.104.1

# AI 调用（DeepSeek 兼容 OpenAI SDK）
openai==2.44.0

# 环境变量
python-dotenv==1.2.2

# 向量数据库（可选依赖：仅启用 RAG_BACKEND=embedding 语义检索时需要）
chromadb>=0.5.0

# Swagger/OpenAPI 文档
drf-spectacular==0.30.0

# LlamaIndex 对照 demo（可选：仅 demo_llamaindex.py 需要）
llama-index==0.14.24
