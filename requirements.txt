# 中渔天下 ZT-agent 依赖清单（主流程）
# 安装：pip install -r requirements.txt
# 建议：Python 3.12+（django-unfold 0.100+ 不再支持 3.11）
# 国内加速（本地开发用）：pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# 注意：Docker 构建已在 Dockerfile 里通过 PIP_INDEX_URL 指向清华源，无需额外参数。

# Web 框架
Django==5.2.17

# 数据库（MySQL）
PyMySQL==1.2.0

# 缓存（生产用 Redis；Django 内置 redis 后端需要此包；未配 REDIS_URL 时回退 LocMem）
redis==5.2.1

# 认证（JWT）
djangorestframework==3.18.0
djangorestframework-simplejwt==5.5.1
PyJWT==2.13.0

# 跨域
django-cors-headers==4.9.0

# 后台管理 UI（Unfold，替代早期 SimpleUI）
django-unfold==0.104.1

# AI 调用（DeepSeek 兼容 OpenAI SDK）
openai==2.44.0

# 环境变量
python-dotenv==1.2.2

# 向量数据库（仅启用 RAG_BACKEND=embedding 语义检索时需要）
chromadb>=0.5.0

# Swagger/OpenAPI 文档
drf-spectacular==0.30.0

# 注意：LlamaIndex 对照 demo 的依赖已移至 requirements-demo.txt
#      （llama-index 会拉入数十个传递依赖，为使 Docker 构建更快，主清单不再包含）
