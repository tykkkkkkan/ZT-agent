# 中渔天下 AI 助手 产品需求文档（PRD）

> 版本：v1.0
> 日期：2026-08-27
> 负责人：产品组
> 状态：评审中

---

## 一、产品概述

| 项目 | 内容 |
|------|------|
| 产品名称 | 中渔天下 AI 助手 |
| 产品形态 | Web 网页（Vue 前端 + Django 后端 + MySQL 数据库） |
| 核心功能 | 智能问答、产品查询、库存查询、报价计算、订单查询、企业知识问答（RAG） |
| 使用场景 | 客户（个体钓友/经销商）和企业老板通过网页对话框，用自然语言咨询产品、查库存、算报价、查订单、问政策 |
| 目标用户 | 个体钓友、经销商、企业内部管理层 |
| 接入方式 | 网页端直接访问，登录后按角色展示对应功能 |

---

## 二、功能列表

| 功能模块 | 具体功能 | 优先级 | 说明 |
|---------|---------|--------|------|
| 用户登录 | 账号登录 / 角色识别 | P0 | 区分钓友、经销商、老板，权限不同 |
| 智能问答 | 自然语言对话入口 | P0 | 大模型 + Agent 调度工具，流式输出 |
| 产品查询 | 按名称查产品信息 | P0 | 返回价格、规格、用法等 |
| 库存查询 | 实时查库存量 | P0 | 返回可用库存及预计到货 |
| 报价计算 | 按数量算总价含折扣 | P0 | 经销商走阶梯价，钓友走零售价 |
| 订单查询 | 按订单号查状态 | P0 | 返回订单状态、物流进度 |
| 企业知识问答 | 基于企业手册 RAG 回答政策类问题 | P1 | 退换货、售后、价格政策等 |
| 销售统计 | 老板看销售/库存/客户数据 | P1 | 图表展示，V1.0 上线 |
| 客户分析 | 客户活跃度、复购率 | P2 | V2.0 上线 |
| 业务员工作量 | AI 处理量统计 | P2 | V2.0 上线 |
| 对话历史 | 保存用户对话记录 | P1 | 便于回溯和审计 |

---

## 三、交互流程

以一个经销商补货咨询为例，完整对话流程如下：

**第 1 轮：开场 + 产品咨询**
- 用户打开网页，登录（角色：经销商），进入对话框
- 用户：「老三样还有多少货？我想补 50 包」
- AI（调用 `check_inventory` + `query_product`）：「老三样（鲤鱼款）当前库存 320 包，规格 200g/包，零售价 8 元，批发价 5.5 元。50 包走批发价，为您计算总价。」

**第 2 轮：报价计算**
- 用户：「算一下 50 包多少钱，有没有更优惠」
- AI（调用 `calculate_quote`）：「50 包批发价 5.5 元/包，总价 275 元。满 100 包可享 9 折（4.95 元/包），如需凑量可考虑 100 包，总价 495 元，更划算。是否下单？」

**第 3 轮：下单 / 查订单**
- 用户：「先下 100 包吧，订单帮我跟踪」
- AI（生成订单 → 调用 `get_order_status`）：「已为您创建订单 #20260827-001，100 包老三样，总价 495 元。当前状态：待付款。付款后预计 2 个工作日发货，我会同步跟踪进度，您可随时回复"查订单"查询。」

**第 4 轮（可选）：政策咨询**
- 用户：「退货政策是什么？」
- AI（调用 `search_knowledge`，基于企业手册 RAG）：「根据《中渔天下售后政策 v2》，饵料类商品因保质期问题，签收 7 天内未开封可申请退货，需联系业务员开具退货单。具体详见售后政策手册第 3 条。」

---

## 四、Agent 工具设计

| 工具名 | 输入 | 输出 | 数据来源 |
|--------|------|------|----------|
| `query_product` | 产品名 / 类别 | 价格、规格、用法、产品图 | MySQL `products` 表 |
| `check_inventory` | 产品名 | 库存量、安全库存、预计到货时间 | MySQL `inventory` 表 |
| `calculate_quote` | 产品名 + 数量 + 用户角色 | 单价、总价、折扣、阶梯建议 | `products` + `price_policy` 表 |
| `get_order_status` | 订单号 | 订单状态、物流进度、预计送达 | MySQL `orders` 表 + 物流接口 |
| `search_knowledge` | 自然语言问题 | 答案片段 + 来源引用 | 向量库（企业手册 RAG） |

**调度逻辑**：AI 根据用户输入判断意图，自动选择一个或多个工具调用，结果汇总后用自然语言回复。多工具场景（如先查库存再算价）由 Agent 串联调用。

---

## 五、非功能性需求

1. **响应时间**：首字返回 ≤ 2 秒（流式输出），单轮完整回复 ≤ 8 秒；工具调用超时 5 秒后降级提示并转人工。
2. **流式输出**：对话采用 SSE 流式返回，先出文字再补数据，避免用户干等；支持打字机效果，可中断。
3. **数据安全**：用户登录态校验；按角色隔离数据（钓友看不到批发价、经销商看不到老板数据）；对话记录脱敏存储，不外泄客户信息。

---

## 六、版本规划

| 版本 | 目标 | 主要功能 | 交付物 |
|------|------|---------|--------|
| V0.1 Demo | 跑通核心链路 | 钓友端：产品查询 + 库存查询 + 报价计算 | 可演示网页 + 假数据 |
| V1.0 | 上线可用 | 全角色登录、5 大核心工具、订单查询、RAG 知识问答、对话历史 | 正式上线网页 + 真实数据 |
| V2.0 | 数据驱动 | 销售统计、客户分析、业务员工作量统计、库存预警推送 | 老板端数据看板 + 预警系统 |

---

## 七、附录

- **技术栈**：前端 Vue 3 + Vite，后端 Django + DRF，数据库 MySQL 8.0，向量库用于 RAG。
- **依赖文档**：《需求分析.md》《企业知识手册》《价格政策表》
- **待确认项**：物流接口对接方式、RAG 知识库内容来源、订单创建是否需对接现有 ERP。
